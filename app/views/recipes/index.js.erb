<% if @recipes.present? %>
  $("#recipes").html("<%= escape_javascript(render partial: 'recipes' ) %>");
<% else %>
  $("#recipes").html("<br><h4>Sorry, there were no results found.</h4>");
<% end %>
